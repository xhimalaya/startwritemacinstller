

def main_add_controller_for_add_operation(event_key=None):
	SW_global.delete_list_divide.clear()
	if((SW_global.current_axes!=None) and(SW_global.current_pos!=None)):
		if(SW_global.current_axes==guideline_axes[l]):
			current_pos



	return 
